import 'package:flutter/material.dart';

const Widget emptyWidget = Center(
  child: Text(
    'データがありません',
    style: TextStyle(
      fontSize: 20,
      color: Colors.black,
      fontWeight: FontWeight.bold,
    ),
  ),
);
